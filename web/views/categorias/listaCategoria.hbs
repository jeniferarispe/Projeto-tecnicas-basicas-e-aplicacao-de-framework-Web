{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{#each acertos}}
    <div class="alert alert-success">{{texto}}</div>
{{else}}

{{/each}}

{{/each}}
<h1>Lista de Categorias: </h1>

{{#each categorias}}  
  <div class="form-row">
    
    <div class="form-group col-md-4">
      <label for="nome">Nome: </label>
      <p class="alert alert-secondary">{{this.nome}}</p>
    </div>
   
  </div>
       <div class="form-group">
        <div class="form-row">
          <div class="form-group col-md-1">
               <form action="/categorias/deletarCategoria" method="post">
                    <input type="hidden" name="id", value="{{_id}}">
                    <button class="btn btn-danger">Deletar</button></a>     
                </form>
          </div>
          <div class="form-group col-md-1">
              <form action="/categorias/editarCategoria" method="post">
                  <input type="hidden" name="id", value="{{_id}}">
                  <button class="btn btn-primary">Editar</button></a> 

              </form>  
          </div>
        </div>
  </div>
 
  <hr>
{{else}}
    <h5>Nenhum categoria cadastro</h5>
{{/each}}
